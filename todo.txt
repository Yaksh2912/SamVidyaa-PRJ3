TODO List

Recently Completed:

1. Optional Task Deadlines
   - Teachers can now choose whether a task has a deadline.
   - When enabled, the teacher can set both the date and time.
   - Student submission behavior now respects overdue tasks.

2. Landing Page Redesign
   - Reworked the hero section into a more polished product-style layout.
   - Replaced oversized metric cards with a single stats ribbon.
   - Added improved spacing, navbar consistency, and hero layout refinements.

3. Desktop App Distribution
   - Added admin upload support for desktop installer files.
   - Added public download availability on the landing page.
   - Added download tracking support for installer downloads.

4. Testimonials Management
   - Added admin-managed testimonials with optional instructor images.
   - Added a landing page testimonials section before the FAQ.
   - Added a carousel with autoplay and manual previous/next navigation.

5. Automated RAG Document Embeddings
   - Added automatic PDF text extraction for uploaded course handouts.
   - Added Pinecone cleanup and re-indexing when a handout is replaced or removed.
   - Added indexing status fields so the course record tracks sync results.

6. Single Module View API
   - Added the backend endpoint to fetch detailed data for a single module.
   - Included module tasks, module test questions, and summary statistics in the response.
   - Replaced the placeholder `GET /api/modules/:id` route with a working implementation.

7. Student Dashboard "History" View
   - Added a dedicated History tab so students can review completed tasks.
   - Added persistent task completion records and a backend history endpoint for student completions.
   - Completed tasks now move out of the active course task list after submission and appear in the history timeline.

Remaining Backlog:

1. Web App Backlog
   - No remaining web-app backlog items are currently listed here.

Out of Scope For This Repo:

1. Desktop app execution, grading, and related secure evaluation workflows
   - Managed separately by the desktop app owner and intentionally excluded from this web app backlog.
